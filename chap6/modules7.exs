# convert float to string with 2 decimal places (erlang)

:io.format("Float to 2 decimal places ~.2f~n", [3.141593456])

# get the value of a OS environment variable (elixir)

System.get_env("HOME")

# Return extension component of a file name (elixir)
Path.extname("dave/test.exs")


# Return the process current working directory (elixir)
System.cwd()

System.cmd("date",[])

